Ты работаешь как senior coding agent над репозиторием mcp_uppaal.

Контекст проекта:
- Репозиторий: uppaal_sdn_isac / mcp_uppaal
- Основная integration-ветка: read
- Текущая задача: Issue #19, P2 integrated single-UAV candidate
- Рабочая ветка: codex/carwasher/19-integrated-candidate
- Base commit: dc7eeb05f1fd3f2a4775428b1cd250363893128d
- Текущий commit: 635c3ba6cd0c5acb3f010e2df949dd89265db9ac
- PR должен направляться в read, не в main.

Что уже сделано:
- Добавлен генератор интегрированной модели в src/uppaal_mcp/integrated/.
- Генератор собирает композицию из 50 процессов:
  - 20 core processes;
  - 8 boundary processes;
  - 22 observers.
- Добавлены:
  - adapt.py;
  - boundary.py;
  - generator.py;
  - inputs.py;
  - replay.py;
  - xmlutil.py;
  - evidence/instantiation/20260908-p2-integrated/.
- Добавлены 18 regression tests для:
  - последовательности PHY-команд;
  - отсутствия преждевременных ACK;
  - admission staging;
  - timeout запроса;
  - возраста KPI.
- Одна диагностическая compile-only проверка UPPAAL завершилась с кодом 0.
- Предыдущая модель сообщала, что полный воспроизводимый evidence-пакет ещё не завершён.
- На Windows/WSL verifyta находится по адресу:
  /mnt/c/Program Files (x86)/UPPAAL-5.0.0/bin/verifyta.exe
- UPPAAL 5.0.0 запускается, но license server иногда даёт Windows socket timeout 10060. При повторном запуске лицензия иногда успешно определяется. Не считать это стабильной работоспособностью без проверки.

Критические правила:
1. Сначала прочитай:
   - AGENTS.md;
   - CONTRIBUTING.md;
   - manifests/v1.md;
   - manifests/collaboration-v1.yaml;
   - manifests/baselines/reviewer-r1.yaml;
   - evidence/instantiation/20260908-p2-integrated/README.md;
   - evidence/instantiation/20260908-p2-integrated/decisions.md.
2. До любых изменений выполни:
   - git status --short;
   - git branch -avv;
   - git log --oneline --decorate -n 10.
3. Не удаляй, не reset-ь, не stash-ь и не перезаписывай чужие изменения.
4. Не изменяй файлы вне write scope Issue #19.
5. Не изменяй manifests без отдельного governance Issue.
6. Не изменяй levels_tex/samplepaper.tex.
7. Все ручные изменения делай через apply_patch.
8. Не называй compile-only проверку verification.
9. Не объявляй P1/P2 принятыми, Gate 1 пройденным или свойства модели verified без соответствующей независимой записи.
10. Не создавай evidence поверх существующего каталога. Для нового запуска используй новый уникальный каталог, например reviewer-run-002.
11. Не используй старые generated artifacts как доказательство, если их hashes не соответствуют текущему commit.

Текущая цель этой итерации:
Завершить Issue #19 технически, получить воспроизводимый evidence-пакет, проверить весь software scope и подготовить качественный PR в read. Gate 1 и научную приёмку не подменять авторским commit.

Рабочий порядок:
1. Проверить состояние checkout и убедиться, что текущие изменения принадлежат Issue #19.
2. Проверить diff относительно base commit.
3. Запустить точечные IntegratedCandidateTests.
4. Запустить полный релевантный test suite.
5. Запустить генератор в новом output-каталоге.
6. Запустить checks.py в новом output-каталоге.
7. Использовать verifyta только если лицензия реально доступна.
8. Сохранить:
   - точные команды;
   - Python version;
   - UPPAAL/verifyta version;
   - commit SHA;
   - source/model/query/generator hashes;
   - instance vector;
   - параметры;
   - environment;
   - hardware;
   - stdout/stderr;
   - exit codes.
9. Запустить audit.py для evidence-пакета.
10. Проверить, что evidence не содержит ложных claims о model checking.
11. Проверить CONTRIBUTING checks.
12. Остановиться и сообщить блокер, если:
   - лицензия нестабильна;
   - тесты падают;
   - generated output недетерминирован;
   - найден семантический дефект;
   - требуется изменение файла вне write scope.
13. После успешной проверки подготовить краткое резюме для PR:
   - что изменено;
   - какие тесты выполнены;
   - какие команды воспроизводят результат;
   - какие ограничения остаются;
   - почему это ещё не Gate 1.
14. Не выполнять merge самостоятельно, если для этого нет явного разрешения.

Критерии готовности Issue #19:
- интегрированный кандидат воспроизводимо генерируется;
- composition 20+8+22 фиксирована и проверяется;
- namespace, channels, clocks и typed ACKs проверены;
- admission, timeout, loss и KPI-age semantics покрыты тестами;
- source/specification/input hashes проверяются;
- output не перезаписывает существующие evidence;
- evidence-пакет аудируется;
- все тестовые failures и ограничения явно зафиксированы;
- PR направлен в read;
- в отчёте ясно указано: P1/P2 independent acceptance, baseline supersession и Gate 1 остаются pending.

Формат работы:
- Не пересказывай план вместо действий.
- Сначала инспектируй состояние, затем выполняй проверки и исправляй только подтверждённые проблемы.
- После каждого существенного этапа сообщай краткий статус.
- В финале дай:
  1. результат;
  2. список изменённых файлов;
  3. команды проверок и их результаты;
  4. evidence path;
  5. оставшиеся blockers;
  6. точный следующий шаг для reviewer/integrator.

В конце напиши промт в ./mcp_uppaal/promts, который логично продолжит работу